interface:
  display_name: "Peerlab"
  short_description: "Repo-specific patterns and workflows for peerlab"
  default_prompt: "Use the peerlab repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true